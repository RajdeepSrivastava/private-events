
<%# <%= turbo_frame_tag 'new_event_form' do %> 
   <h1>Create a New Event</h1> 

  <%= form_for @event, url: events_path do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :date %>
    <%= form.date_field :date %>
  </div>
  <div>
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <%= form.submit 'Create Event' %>
<% end %>

